[package]
name = "dslab-faas"
version = "0.0.1"
license = "MIT OR Apache-2.0"
edition = "2021"

[dependencies]
dslab-core = { path = "../dslab-core" }
num = "0.2.0"
rand = "0.8"
rand_pcg = "0.3.1"
serde = { version = "1.0", features = ["derive"] }
