[package]
name = "network-topology-benchmark"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2021"

[dependencies]
dslab-core = { path = "../../crates/dslab-core" }
dslab-network = { path = "../../crates/dslab-network" }
log = "0.4"
env_logger = "0.10.0"
serde = { version = "1.0", features = ["derive"] }
sugars = "3.0.0"
clap = { version = "4.1.9", features = ["derive"] }
