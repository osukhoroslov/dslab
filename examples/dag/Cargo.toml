[package]
name = "dag_example"
version = "0.1.0"
edition = "2021"

[dependencies]
core = { path = "../../core" }
compute = { path = "../../compute" }
network = { path = "../../network" }
dag = { path = "../../dag" }
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
sugars = "3.0.0"
