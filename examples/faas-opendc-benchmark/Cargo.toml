[package]
name = "faas-opendc-benchmark"
version = "0.0.1"
edition = "2021"

[dependencies]
simcore = { path = "../../crates/simcore" }
serverless = { path = "../../crates/serverless" }
serverless-extra = { path = "../../crates/serverless-extra" }
